import { describe, it, expect } from 'vitest'

// This tests slug generation utility — to be implemented in packages/core/src/utils/slug.ts
// Import will fail until the utility is created (RED phase)
// import { generateSlug } from '../utils/slug'

describe('generateSlug', () => {
  it.todo('converts spaces to hyphens')
  it.todo('removes accents from Spanish characters')
  it.todo('truncates to 40 characters')
  it.todo('converts to lowercase')
  it.todo('removes special characters except hyphens')
})
