# MVP Notes

Early product ideas and static prototypes live here so the repository root stays clean.

Use `docs/mvp/` for tracked discovery artifacts. Use `.artifacts/screenshots/` for local
screenshots and throwaway visual captures; that folder is intentionally ignored by Git.
