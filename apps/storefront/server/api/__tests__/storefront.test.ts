import { describe, it, expect } from 'vitest'

describe('storefront API contract', () => {
  it.todo('returns business data for valid active slug')
  it.todo('returns 404 for unknown slug')
  it.todo('returns 410 for suspended business')
})
