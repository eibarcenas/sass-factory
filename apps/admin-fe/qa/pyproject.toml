[project]
name = "admin-fe-qa"
version = "0.1.0"
description = "Screenplay-pattern E2E QA automation for admin-fe (behave + Playwright)"
requires-python = ">=3.13"
dependencies = [
    "behave>=1.2.6",
    "playwright>=1.48.0",
]

[tool.uv]
package = false
