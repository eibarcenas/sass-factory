import { describe, it, expect } from 'vitest'

describe('auth global middleware', () => {
  it.todo('redirects unauthenticated user to /login')
  it.todo('allows authenticated user to proceed')
  it.todo('skips redirect for /login page')
})
