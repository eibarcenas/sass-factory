export default defineNuxtConfig({
  compatibilityDate: '2025-01-01',
  vite: {
    plugins: [
      // Module Federation remote config will be added when @module-federation/vite is confirmed
      // For now this is the skeleton
    ],
  },
})
